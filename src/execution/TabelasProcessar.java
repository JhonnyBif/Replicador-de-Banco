package execution;

public class TabelasProcessar {
	public static void execute(int id_processo) {
		
		System.out.println(id_processo);
		// select * from tb_processo_tabela where id_processo = ? and habilitado order by ordem;
		
	}
}
